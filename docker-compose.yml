services:
  postgres:
    image: postgres:16

    environment:
      POSTGRES_DB: bank
      POSTGRES_USER: bruna
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}

    ports:
      - "5432:5432"
    volumes:
      - bank-data:/var/lib/postgresql/data

volumes:
  bank-data: